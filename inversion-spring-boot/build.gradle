dependencies {

	api project(':inversion-api')

	api "org.springframework.boot:spring-boot-starter-web:${springBootVersion}"
	api "org.springframework.boot:spring-boot-starter-actuator:${springBootVersion}"
	api "org.springframework.boot:spring-boot-starter-jdbc:${springBootVersion}"
	api "net.rakugakibox.spring.boot:logback-access-spring-boot-starter:${logbackSpringBootStarter}"
	

}


task run(type:JavaExec) {
	main = "io.inversion.utils.TestUploads"
	classpath = sourceSets.main.runtimeClasspath
	systemProperties System.getProperties()
}